// React Hooks
import {useState, useContext} from "react";

// MUI Components
import {Stack} from "@mui/material";

// MUI-Joy Components

const Home = () => {
  return (
    <Stack sx={{height: "105vh", width: "100%"}}>
      <h1>Home</h1>
    </Stack>
  );
};

export default Home;
